import {Metadata} from "next";

export const metadata: Metadata = {
    title: 'Friends'
}
const Friends = () => {
    return (
        <div>
            Friends page
        </div>
    );
};

export default Friends;